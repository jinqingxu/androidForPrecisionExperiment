package com.example.precisionexperimentapp;

public class zzzExtraCode {

	 /*
    private void emailResult() {

        Mail email = new Mail("actlab.targetselection", "FittsTask3661", "Finger Calibration Task PId " + pid, "Results of Finger Calibration Task from PId " + pid + ".");

        // Add the "PId_" + pid + "_FingerCalibData.csv" file as an Attachment

        try {

            email.addAttachment(getFilesDir().getAbsolutePath() + "/PId_" + pid + "_FingerCalibData_Internal.csv");

        } catch (Exception e1) {
            e1.printStackTrace();
            Toast.makeText(LoginScreen.this,"Cannot Attach a File ... ",Toast.LENGTH_LONG).show(); // displays a message
        }


        try {
            if(email.send())
                Toast.makeText(LoginScreen.this,"Sending Results ... ",Toast.LENGTH_LONG).show(); // displays a message

            else
                Toast.makeText(LoginScreen.this,"No Luck ... ",Toast.LENGTH_LONG).show(); // displays a message

        } catch (Exception e) {

            e.printStackTrace();
            Toast.makeText(LoginScreen.this,"Cannot Send Results ... ",Toast.LENGTH_LONG).show(); // displays a message
        }
    }
    
            //paint.setColor(Color.BLACK);
        //canvas.drawLine(0, (int) startY, screenWidth, (int) startY, paint);
        //canvas.drawLine(screenWidth/2, 0, screenWidth/2, screenHeight, paint);

        //canvas.drawLine(0, 0, (int) startX, (int) startY, paint);
        //canvas.drawLine((int) startX, (int) startY, screenWidth, (int) startY * 2, paint);
        //canvas.drawLine(0, relLayout.getBottom(), screenWidth, 0, paint);



    */
}
